{
  "name": "design-gpt-qa",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npx bddgen && playwright test",
    "test:ui": "npx bddgen && playwright test --ui",
    "test:headed": "npx bddgen && playwright test --headed",
    "test:fast": "npx bddgen --config playwright.fast.config.js && playwright test --config playwright.fast.config.js",
    "test:render": "npx bddgen --config playwright.render.config.js && playwright test --config playwright.render.config.js",
    "test:workflow": "npx bddgen --config playwright.workflow.config.js && playwright test --config playwright.workflow.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "jsonwebtoken": "^9.0.0",
    "playwright-bdd": "^8.4.2"
  }
}
