{
  "name": "design-gpt",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "cacheDirectories": ["app/node_modules"],
  "scripts": {
    "build": "./bin/build",
    "heroku-postbuild": "./bin/build"
  }
}
