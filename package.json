{
  "private": true,
  "engines": { "node": "20.x" },
  "cacheDirectories": ["app/node_modules"],
  "scripts": {
    "heroku-postbuild": "cd app && npm ci && npm run build && cp -r dist/* ../api/public/"
  }
}
